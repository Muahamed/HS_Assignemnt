spring:
  datasource:
    url: jdbc:h2:file:~/hunger-2-helm;AUTO_SERVER=TRUE
    initialization-mode: always
    platform: h2
  jpa:
    show-sql: true
    generate-ddl: true
    hibernate.ddl-auto: create-drop
